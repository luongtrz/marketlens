app:
  name: StockMem
  host: 0.0.0.0
  port: 8000

storage:
  db_url: sqlite+aiosqlite:///test.db

search:
  vector_backend: memory
  top_k_default: 5
  weights:
    w1_factor: 0.35
    w2_indicator: 0.20
    w3_price: 0.45
